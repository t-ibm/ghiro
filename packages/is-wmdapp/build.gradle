project.label = 'WmDApp'
project.description = "$label package"

dependencies {
    compile project(":modules:$project.rootProject.name-contract")
}

pkg {
    manifest.values(
        'name': project.label,
        'description': project.description,
    )
}
